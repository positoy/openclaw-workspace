- Andy prefers system-level or direct-behavior changes to be applied in `~/.openclaw/openclaw.json`, not in `USER.md`.
- Short rule to remember: "시스템 설정은 openclaw.json 우선."
- For PDF extraction, use the OpenClaw `pdf` tool workflow, not ad-hoc file parsing.
- `kordoc` is a useful local converter for HWP/HWPX/PDF to Markdown, but it may require the proper local dependency/runtime setup instead of `npx` temp execution.
- In the `gpt-research/gwangju` project, discovered documents should be tracked in `report.md`, with originals in `files/`, extracted text in `scripted/`, and summaries in `summary/`.
- Andy wants concise, structured analysis for current events and can ask for comparisons between political claims and news reports.
- Andy cares about the openness of the Hormuz Strait from South Korea's perspective and is interested in risk premium / shipping / energy cost implications.
