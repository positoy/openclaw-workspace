- Andy clarified that all real projects live under `/Users/positoy/github`; treat “project”/“github project” as defaulting there.
- Began `/Users/positoy/github/gpt-research/x-scraper` as a Node + Playwright MVP for scraping X profiles and producing hourly summaries, focusing on `@Alisvolatprop12` and `@otaku__yuta`.
- Andy prefers software/dev/research tasks to follow plan -> revise -> explicit "실행" before execution, but also directly requested many concrete file/config changes during this session.
- For X scraping, current MVP can collect visible posts but feed matching is imperfect due to pinned posts / timeline UI behavior; Playwright session reuse via `storage-state.json` was introduced conceptually.
- In `gpt-research/gwangju`, collected official Gwangju station-area development references and created `gwangju/report.md` as the working summary document.
- Downloaded/organized official Gwangju-related files and Andy prefers Korean filenames with `YYYYMMDD_파일명` format.
- HWP->PDF conversion on this macOS + Python 3.9 environment is currently blocked by tool/runtime limitations; `simple-hwp2pdf` and similar Python paths were not reliable here, so LibreOffice or another path may be needed.
- Andy wants OpenClaw PDF analysis enabled via explicit `agents.defaults.pdfModel`; current target model is `openai-codex/gpt-5.4`.
